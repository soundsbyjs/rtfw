#! /bin/sh

# change the path to the location of the python file then
# sudo mv rtfw.sh /usr/local/bin/rtfw
# sudo chmod +x /usr/loca/bin/rtfw

# this is not the only way to install it

cd [path to scraper.py]
python3 scraper.py $1 $2 $3
